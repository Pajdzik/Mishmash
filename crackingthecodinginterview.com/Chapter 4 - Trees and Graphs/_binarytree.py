#!/usr/bin/env python

class Node:
    def __init__(self, value):
        self.value = value
        self.left = None
        self.right = None

def add_node(node, value):
    # if (value > node.value):


def create_binary_tree(nodes):
    root_node = nodes[0]

    for node in nodes[1:]:


if __name__ == "__main__":
